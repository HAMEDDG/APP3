org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# NOTE: Gradle 7+ / Gradle wrapper 8.12 require a Java 11+ runtime.
# If your build fails with "Dependency requires at least JVM runtime version 11. This build uses a Java 8 JVM",
# either install a JDK 11+ and set the system JAVA_HOME to that JDK, or set Gradle's java home here.
# To set it here, uncomment and provide the absolute path to your JDK 11+ installation (Windows example):
# org.gradle.java.home=C\:\\Program Files\\Java\\jdk-11.0.20

# Leave commented unless you want this project to force Gradle to use a specific JDK installation.
